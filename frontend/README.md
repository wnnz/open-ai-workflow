# Frontend

```powershell
. ..\scripts\enable-proxy.ps1
npm install
npm run dev
```

The UI uses locally owned Volt wrappers in `src/volt`, Tailwind CSS 4, light/dark themes, and Chinese/English translations.
